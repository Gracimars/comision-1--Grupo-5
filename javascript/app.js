// const saludos = require("./saludos.js");

// Sintaxis de desestructuración y require
const { saludar, saludarHolaMundo } = require("./saludos.js");

console.log(saludar("Gracimar y Andrés"));
console.log(saludarHolaMundo());
